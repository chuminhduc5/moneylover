import 'package:flutter/material.dart';

class MoneyConfigScreen extends StatelessWidget {
  const MoneyConfigScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
